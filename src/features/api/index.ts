export const test1 = 123;
// import {
//   createEvent,
// } from 'effector';
// import axios from 'axios';

// export const createApi = ({ baseUrl, endpoints, headers }: ApiType) => {
//   return Object.keys(endpoints).map((endpoint) => `${endpoint} ${baseUrl} ${headers}`);
// };

// const request = createEvent();

// const buildingApi = createApi({
//   baseUrl: '/',
//   endpoints: {
//     getBuildings: {
//       query: '/buildings',
//       payload: (channelId: string) => ({ channelId }),
//     },
//   },
//   headers: { 'X-Custom-Header': 'foobar' },
// });
